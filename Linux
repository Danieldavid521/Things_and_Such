Install Ubuntu server
Setup DHCP with netplay
Mount /dev/sdb1 to /home/Daniel/sambashare
Install samba
Reformat drive from ext4 to nfs
Tried to set files and samba share folder owned by user:group


key virtual system information is in /proc, and are virtual so are dynamic 

-meminfo
-cpuinfo

top - single auto updated for CPU and men
free -h 
df -ht

masking a service is a fail proof command to help stop the service from being accidentally started/stopped

Berkely internet name domain - most used DNS service

port 53 is for DNS lookups
port 953 is for controlling DNS 