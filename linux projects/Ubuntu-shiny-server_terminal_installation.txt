installed ubnutu server 21.
install openssh-server
installed postgressql

sudo apt install unattended-upgrades
sudo adduser
#add admin privs to user
sudo usermod -a -G sudo [username]
#see users and system accounts
compgen -u
hostname
hostnamectl
sudo systemctl status ssh
sudo systemctl enable --now ssh
#list drives
lsblk

#checking firewall rules
ufw app list
sudo ufw status
sudo apt install ufw
sudo ufw enable

#install nginx
sudo apt install nginx 
sudo service nginx restart

'htop' command helps monitor the server

#installing R
sudo apt-key adv --keyserver keyserver.ubuntu.com --recv-keys E298A3A825C0D65DFD57CBB651716619E084DAB9
sudo add-apt-repository 'deb https://cloud.r-project.org/bin/linux/ubuntu focal-cran40/'
sudo apt update
sudo apt install r-base
# start R as root, so packages are avaiable for all users
sudo -i R
install.packages("x")

#Installing nginx
sudo apt install nginx
sudo ufw app list
sudo ufw allow 'Nginx HTTP'
sudo ufw status
systemctl status nginx
sudo systemctl enable nginx
#might need to lookup additional configs for ownership of /var/www/site
#test if any symlinks are broken
sudo nginx -t
# default config file for nginx /etc/nginx/nginx.conf

#install shiny server
sudo su - -c "R -e \"install.packages('shiny', repos='http://cran.rstudio.com/')\""
wget https://download3.rstudio.org/ubuntu-18.04/x86_64/shiny-server-1.5.18.987-amd64.deb
sha256sum shiny-server-1.5.18.987-amd64.deb
sudo apt update
sudo apt install gdebi-core
sudo gdebi shiny-server-1.5.17.973-amd64.deb
#default port is 3838
sudo ss -plut | grep -i shiny 
sudo ufw allow 3838
sudo nano /etc/nginx/nginx.conf
sudo nano /etc/nginx/sites-available/example.com
#below command prevent default nginx page to load
sudo unlink /etc/nginx/sites-enabled/default
sudo su - -c "R -e \"install.packages('rmarkdown', repos='http://cran.rstudio.com/')\""
#checking shiny server what ports are open
sudo ufw status numbered
#remove access to shiny server on port 3838
sudo ufw delete 3(or whatever number shiny is on)
#reload firewall rules
sudo ufw reload